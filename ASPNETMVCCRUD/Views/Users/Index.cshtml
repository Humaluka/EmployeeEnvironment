@{
    ViewData["Title"] = "Список пользователей";
}

<h1>Пользователи</h1>
<table class="table">
    <thead>
        <tr>
            <th>Идентификатор</th>
            <th>Логин</th>
            <th>Email</th>
            <th>Роль</th>
            <th>Страна</th>
            <th>Действия</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.UserID</td>
                <td>@item.Username</td>
                <td>@item.Email</td>
                <td>@item.RoleID</td>
                <td>@item.CountryID</td>
                <td>
                    <form asp-action="UserProfile" asp-controller="Users" method="get" style="display: inline;">
                        <input type="hidden" value="@item.UserID" name="userID" />
                        <button type="submit" class="text-danger">Редактировать</button>
                    </form>
                    <form asp-action="DeleteUser" asp-controller="Users" method="post" style="display: inline;">
                        <input type="hidden" value="@item.UserID" name="userID" />
                        <button type="submit" class="text-danger">Удалить</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

<div class="text-center">
    <a asp-action="Create" class="btn btn-primary">Новый пользователь</a>
</div>